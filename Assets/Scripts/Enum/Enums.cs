public enum ToolEffect
{
    none,
    watering
}

public enum ItemType
{
    Seed,
    Commodity,
    Watering_tool,
    Hoeing_tool,
    Chopping_tool,
    Breaking_tool,
    Reaping_tool,
    Collecting_tool,
    Reapable_scenary,
    Furniture,
    Outfit,
    none,
    count
}

public enum Outfit
{
    Default,
    Farmer,
    none,
    count
}

public enum NPCType
{
    Shop,
    JustTalk,
    count
}

public enum InventoryLocation
{
    player,
    chest,
    shop,
    count
}

public enum SceneName
{
    Scene_Farm_1,
    Scene_Home,
    Scene_Field
}
